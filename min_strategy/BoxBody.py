def box_body(self):
    # if self.__board_point['max_min_price'] >= 5:
    #     max_min_price = np.array(self.__board_point['max_min_price'])
    #     cross_direc = np.array(self.__board_point['cross_direc'])
    #     max_price = max_min_price[cross_direc == -1]
    #     min_price = max_min_price[cross_direc == 1]
    return